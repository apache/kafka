package com.uber.kafka.tools;

import org.junit.Test;

public class KafkaMigrationToolTest {

    @Test
    public void testFoo() throws Exception {
        // InputStream is = getClass().getResourceAsStream("/offset_idx");
        // byte[] encodedOffsets = ByteStreams.toByteArray(is);
        // Assert.assertEquals(0, encodedOffsets.length);
    }

}

